# ============================================================
# OSCAR Shareable Environment Configuration
# ============================================================
#
# Copy this file to .env and modify as needed.
#
# ============================================================

# ============================================================
# Core Configuration
# ============================================================
#
# BASE_HOSTNAME: The public-facing hostname for the application.
# e.g., test.sdbfriends.ca
#
BASE_HOSTNAME=test.sdbfriends.ca

# DOCKER_GID: The group ID of the docker group on the host. 
# This is usually 999 or 1001. Find yours with `getent group docker`.
#
DOCKER_GID=999

# UID and GID for the user running the containers. 
# In rootless mode, this should be 0 unless you have specific needs.
#
TARGET_UID=0
TARGET_GID=0

# XDG_RUNTIME_DIR: Usually /run/user/<UID>
# Used for Docker rootless sockets.
#
XDG_RUNTIME_DIR=/run/user/1000

# ============================================================
# Uploader Configuration
# ============================================================
#
# STORAGE_PATH: Where uploaded files are stored temporarily.
#   This must be accessible by both the uploader and OSCAR.
#
STORAGE_PATH=/config/dev/oscar-shareable/data/uploads

MAX_UPLOAD_BATCH_BYTES=1073741824   # 1 GB
OSCAR_VERSION=latest              # Version number (e.g., 1.7.1) or 'latest'
OSCAR_DOCKER_IMAGE=custom-oscar-vnc:latest
OSCAR_PROFILE_VERSION=1.7.1+-plus
OSCAR_IDLE_TIMEOUT_SECONDS=300    # Idle timeout for VNC container in seconds

# ============================================================
# Discord OAuth
# ============================================================
# DISCORD_CLIENT_SECRET is handled securely via Docker secrets.
# Please put your Discord Client Secret string in the 
#   ./secrets/discord_client_secret.txt
# file before deploying.
#
DISCORD_CLIENT_ID=            # The Discord Application Client ID ("snowflake")
DISCORD_REDIRECT_URI=         # e.g., https://app.example.com/api/auth/discord/callback

# ============================================================
# OSCAR / Selkies UI Configuration
# ============================================================
SELKIES_ENABLE_CURSORS=true
SELKIES_AUDIO_ENABLED=false
SELKIES_GAMEPAD_ENABLED=false
SELKIES_UI_SIDEBAR_SHOW_GAMEPADS=false
SELKIES_UI_SIDEBAR_SHOW_CLIPBOARD=false
SELKIES_UI_SIDEBAR_SHOW_AUDIO_SETTINGS=false
SELKIES_UI_SIDEBAR_SHOW_SHARING=false
SELKIES_MICROPHONE_ENABLED=false
SELKIES_CLIPBOARD_IN_ENABLED=false
SELKIES_CLIPBOARD_OUT_ENABLED=true
SELKIES_SECOND_SCREEN=false
SELKIES_ENABLE_SHARING=false
SELKIES_UI_SHOW_CORE_BUTTONS=false
SELKIES_USE_BROWSER_CURSORS=true
MAX_RESOLUTION=2560x1440
