{
  "name": "secure-uploader",
  "version": "3.0.0",
  "description": "Hardened OSCAR SD card uploader",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/help-tabs.js && node scripts/check-csp-compliance.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2"
  }
}
