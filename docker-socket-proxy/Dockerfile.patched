FROM tecnativa/docker-socket-proxy:latest
USER root
RUN apk add --no-cache --upgrade \
    libssl3>=3.3.6-r0 \
    libcrypto3>=3.3.6-r0 \
    pcre2>=10.46-r0
# Removed USER nobody to match the base image's expected environment
